-- ============================================================
-- iARTESANA Sistema Base — Clear descriptions for blocks 11 and 12
-- ============================================================

UPDATE sb_block_definitions
SET description = ''
WHERE id IN (11, 12);
